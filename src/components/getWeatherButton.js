import React, {Component} from 'react';

class GetWeatherButton extends Component {
    render() {
        return (
            <button style={{marginTop: 20}}>Get Weather</button>
        );
    }
}

export default GetWeatherButton;
